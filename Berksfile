source 'https://supermarket.chef.io'

metadata

# dependencies required for running the Kitchen test suite
group :test do
  cookbook 'dkron-test', path: 'test/fixtures/cookbooks/dkron-test'
end
